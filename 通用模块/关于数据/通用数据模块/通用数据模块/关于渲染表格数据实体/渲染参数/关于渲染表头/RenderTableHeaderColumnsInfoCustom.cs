namespace System.DataFrancis;

/// <summary>
/// 这个记录是用来渲染表格表头中的一列的参数，
/// 它表示这一列是虚拟的，没有直接对应的属性
/// </summary>
public sealed record RenderTableHeaderColumnsInfoCustom : RenderTableHeaderColumnsInfoBase
{
}
