@namespace Microsoft.AspNetCore.Components
@inherits ComponentBase
@typeparam Obj

@if (ChildContent is { })
{
    <div @onclick=OnSelect @attributes=InputAttributes>
        @ChildContent((IsSelect,Value))
    </div>
}
