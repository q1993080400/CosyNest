@namespace Microsoft.AspNetCore.Components
@inherits ComponentBase
@inject IJSWindow JSWindow
@inject BrowserFileConvert BrowserFileConvert

@if (UploadTaskInfo is { })
{
    @RenderComponent(GetRenderInfo())
}
