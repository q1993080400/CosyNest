@namespace BootstrapBlazor.Components
@inherits ComponentBase

@{
    var files = RenderPreviewInfo.Files;
}
@if (files.Count > 0)
{
    <BootstrapFileViewer Files="files" OnClickEventPreference=" RenderSingleFileEventPreference.Cancel" />
}